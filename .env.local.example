# Next.js frontend — local dev environment
# Copy to .env.local (gitignored). Never commit .env.local.

# Backend API base URL called by the browser.
# In local dev this points to the Docker backend container.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api/v1
